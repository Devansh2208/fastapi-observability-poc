apiVersion: v1
kind: Service

metadata:
  name: fastapi-service
  namespace: observability-poc

  labels:
    App: fastapi-observability

spec:
  selector:
    App: fastapi-observability

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000

  type: LoadBalancer