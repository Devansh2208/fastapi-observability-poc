apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: observability-poc
  region: ap-south-1

managedNodeGroups:
  - name: workers-small
    instanceType: t3.small
    desiredCapacity: 1
    minSize: 1
    maxSize: 2